import React from "react";import { describe, it, expect, vi } from "vitest";vi.mock("react", async (importOriginal) => {  const actual = await importOriginal<typeof import("react")>();  return {    ...actual,    useState: <T,>(initial: T) => [initial, vi.fn()],    useEffect: vi.fn(),  };});vi.mock("@/components/wallet-provider", () => ({  useWallet: () => ({ address: "GAAZI4TCR3TY5OJHCTJC2A4QSY6CJWJH5IAJTGKIN2ER7LBNVKOCCWN7" }),}));vi.mock("@/hooks/use-accrual", () => ({  useAccrual: () => ({ withdrawable: 0n, vested: 0n }),}));vi.mock("next/link", () => ({  default: ({ children }: { children: React.ReactNode }) => <a>{children}</a>,}));vi.mock("next/navigation", () => ({  useParams: () => ({ id: "123" }),}));vi.mock("@/components/stream-actions", () => ({  StreamActions: () => <div>StreamActions</div>,}));import { StreamDetail } from "@/components/stream-detail";import { formatTime } from "@/lib/format";import { formatUtcFromUnixSeconds } from "@/lib/timezone";import type { StreamView } from "@/types/stream";describe("StreamDetail", () => {  const baseStream: StreamView = {    id: "123",    sender: "GAAZI4TCR3TY5OJHCTJC2A4QSY6CJWJH5IAJTGKIN2ER7LBNVKOCCWN7",    recipient: "GBBZI4TCR3TY5OJHCTJC2A4QSY6CJWJH5IAJTGKIN2ER7LBNVKOCCWN7",    token: "USDC",    totalAmount: "10000000000",    withdrawn: "0",    vested: "0",    withdrawable: "0",    locked: "10000000000",    startTime: "1700000000",    endTime: "1700003600",    cliffTime: "1700001000",    cancelled: false,    status: "streaming",    progress: 10,  };  it("renders the schedule fields matching documented display format", () => {    const el = StreamDetail({ stream: baseStream, onComplete: vi.fn() });    const output = JSON.stringify(el);    expect(output).toContain("Start");    expect(output).toContain(formatTime(baseStream.startTime));    expect(output).toContain(formatUtcFromUnixSeconds(baseStream.startTime));    expect(output).toContain("End");    expect(output).toContain(formatTime(baseStream.endTime));    expect(output).toContain(formatUtcFromUnixSeconds(baseStream.endTime));    expect(output).toContain("Cliff");    expect(output).toContain(formatTime(baseStream.cliffTime));    expect(output).toContain(formatUtcFromUnixSeconds(baseStream.cliffTime));  });});